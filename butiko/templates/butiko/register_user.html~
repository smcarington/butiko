{% extends 'butiko/base.html' %}

{% block content %}
    <h2>Welcome to Butiko!</h2>

    {% if error %}
        {% for key, val in error.items %}
            {{val}}
        {% endfor %}
    {% endif %}

    <form method="POST" class="post-form">{% csrf_token %}
        {% for field in form %}
            <div class="fieldWrapper">
                {{ field.error }}
                {{ field.label_tag }} {{field}}
            </div>
        {% endfor %}
        
        <div class="fieldWrapper">
            <label for="id_pass_conf">Confirm:</label>
            <input id="id_pass_conf" max_length="128" name="passconf" type="password">
        </div>

        <button type="submit" class="save btn btn-default" id="register-user">Save</button>
    </form>
{% endblock %}
